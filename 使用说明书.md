# 微信消息抓取 Skill · 使用说明书

> 版本：v1.1 ｜ 更新：2026-08-23
> 定位：**通用版微信消息抓取工具**——不内置任何业务规则，安装者通过回答提问完成配置，即可自动抓取指定微信群/联系人的消息（支持按发送者/关键词过滤），定时汇总并直发到指定微信。

---

## 一、这个 Skill 能实现什么功能

| 功能 | 说明 |
|---|---|
| 📥 自动抓取 | 挂机轮询，定时读取**指定微信群/联系人的最新消息**（可多个目标） |
| 🎯 按发送者过滤 | 只抓群里**特定人的全部发言**（如"抓张三组长所有发言"） |
| 🔑 按关键词过滤 | 消息命中**任一关键词**才抓取（可留空=全部抓） |
| 🚫 排除词过滤 | 命中**任一排除词**的消息直接丢弃（如广告/闲聊） |
| 💾 增量落库 | 消息存 SQLite，内容 hash 去重；遇已入库消息即停（增量边界，不重复扫旧消息） |
| 📅 时间窗口 | 只抓最近 N 天内的消息（默认 3 天，可配） |
| 📊 定时汇总 | 按时间窗口生成**原文版汇总**（不简化），跨群归一化去重 |
| 📤 微信直发 | 汇总自动分段（≤1800 字/条）直发到指定微信（文件传输助手/联系人/群） |
| 🛡️ 反封号 | 夜间停轮、随机采样、随机抖动、模拟人类节奏、锁微信版本 |
| 💓 心跳监控 | 探针检查微信进程/版本/wxauto 链路，掉线可告警 |

## 二、实现途径：用的什么工具和机制

### 技术栈

| 层 | 工具/机制 | 说明 |
|---|---|---|
| 自动化 | **wxauto4 41.1.2**（Python 库） | 操作 PC 微信的 UI 自动化库，基于 Windows UIA（UI Automation）控件树，**零协议风险**（不碰微信通信协议，不注入） |
| 宿主 | **微信 PC 版 4.1.7.30** | 唯一兼容版本：此版本消息列表暴露 UIA 控件；4.1.12+ 自绘 UI 无法自动化 |
| 语言 | **Python 3.11** | 脚本运行环境 |
| 存储 | **SQLite**（标准库 sqlite3） | 消息落库，`db/messages.db`，表 messages（group/content/msg_hash 唯一/hit_keywords/sender_nick/created_at） |
| 调度 | Windows 计划任务 / `--loop` 常驻 | 定时触发轮询；OpenClaw cron 可管汇总与心跳 |

### 核心机制（怎么读微信消息的）

```
微信窗口（UIA 控件树）
  └─ ChatWith("群名") 搜索式打开会话（不依赖会话列表，折叠群也能开）
      └─ GetAllMessage() 读取可见消息
          ├─ 每个消息对象带 content（内容）、sender（发送者群内昵称）、time（时间标签）
          └─ 失败回退：直接 UIA 枚举 chat_message_list 控件的 ChatItemView.Name（纯文本，无发送者）
              └─ 时间过滤：靠 system 时间标签行（YYYY-MM-DD）分段，超过 time_range_days 的跳过
```

### 三大过滤机制（按顺序执行）

```
读到的每条消息
  ├─ ① 发送者过滤：sender ∈ rules.senders？（空=所有人）【2026-08-23 新增】
  ├─ ② 关键词过滤：命中任一 rules.keywords？（空=全抓）
  ├─ ③ 排除词过滤：不命中任一 rules.exclude_keywords？（空=不排）
  └─ 通过 → SQLite 入库（内容+群 hash 去重）
```

### 防封号机制（7 道，全参数可调）

| 机制 | 默认值 | 原理 |
|---|---|---|
| 🌙 夜间停轮 | 23:00-6:00 | 深夜不轮询，避开风控高峰 |
| 🎲 随机采样 | 70% | 每轮只随机读部分目标，打乱访问规律 |
| ⏱️ 群间休息 | 3-8 秒随机 | 模拟人类切群节奏，不机械连点 |
| 🎯 轮询抖动 | ±10 分钟随机 | 固定周期 + 随机偏移，不规律化 |
| 🚫 关点资料卡 | 默认关 | 高频点击头像弹窗是风控高危操作（需微信号才开，且仅好友可见） |
| 📅 时间窗口 | 用户配 | 只读近 N 天，减少操作量 |
| 🔒 锁版本 | 心跳检查 | 微信禁升级（4.1.7.30 才暴露控件） |

## 三、文件清单（专用文件夹内）

```
wechat-message-capture/
├── SKILL.md                        # Skill 主文档（OpenClaw 识别入口）
├── 使用说明书.md                   # 本文档
├── config.json                     # 运行配置（安装者填写）
├── wechat_client.py                # 微信连接/读消息核心（wxauto4 封装）
├── pipeline.py                     # 轮询主流程（抓取→过滤→入库）
├── filter.py                       # 过滤规则引擎（从 config 编译规则）
├── store.py                        # SQLite 落库 + 去重
├── gen_wechat_summary.py           # 汇总生成
├── split_wechat_summary.py         # 汇总分段（≤1800 字）
├── send_via_wxauto.py              # 单条直发
├── send_wechat_summary.py          # 一键：生成+分段+直发
├── wx_probe.py                     # 心跳探针（进程/版本/wxauto 三检查）
├── check_db.py                     # 查库调试
├── diag_sender.py                  # 诊断：看某群消息的发送者字段
├── scripts/config.example.json     # 配置模板（空白引导式）
└── references/安装配置说明书.md     # 安装问答清单（精简版）
```

## 四、如何使用

### 第 1 步：环境准备（一次性）

| 依赖 | 要求 |
|---|---|
| Windows | 10/11 |
| 微信 PC 版 | **4.1.7.30**（必须！装好关闭自动升级） |
| Python | 3.11 |
| wxauto4 | `pip install wxauto4`（版本 41.1.2） |
| 电脑 | 可长期开机，微信保持登录且窗口不最小化到托盘 |

### 第 2 步：回答配置问题（写入 config.json）

见 `references/安装配置说明书.md` 的完整问答清单，核心 5+1 问：

1. **抓哪些群/人？** → `targets.groups`（群名）/ `targets.contacts`（联系人）
2. **只抓特定人发言？** → `rules.senders`（群内昵称，可多个）
3. **关键词/排除词？** → `rules.keywords` / `rules.exclude_keywords`（可留空）
4. **多久监测一次？** → `poll.interval_seconds`（秒；建议 ≥600）
5. **汇总发哪？几发？几点？** → `summary.send_to` / `frequency` / `schedule`
6. **抓几天内？** → `time_range_days`（默认 3）

### 第 3 步：测试

```bash
# ① 探针自检：微信在线？版本对？wxauto 可用？
python wx_probe.py

# ② 单轮抓取（--verbose 看详细过程）
python pipeline.py --once --verbose

# ③ 查看抓到了什么
python check_db.py --days 2

# ④ 生成汇总并直发（先发自己确认）
python send_wechat_summary.py --who 文件传输助手
```

### 第 4 步：挂机

- **方式 A（推荐）**：Windows 计划任务，每 `interval_seconds` 跑一次 `pipeline.py --once`（防重叠）
- **方式 B**：`python pipeline.py --loop` 常驻（自带随机抖动）
- 汇总定时：计划任务或 OpenClaw cron 在 `summary.schedule` 时刻跑 `send_wechat_summary.py`

### 第 5 步：日常维护

- 看日志：`logs/pipeline_YYYYMMDD.log`
- 看库：`python check_db.py`
- 调规则：改 config.json 后无需重启，下轮自动生效
- 微信升级提示 → 立即用 `wx_probe.py` 检查版本，若变化需降回 4.1.7.30

## 五、故障排查

| 症状 | 排查 |
|---|---|
| 抓不到消息 | ① `wx_probe.py` 确认微信在线+版本 ② 群名和搜索框输入完全一致（含 emoji）③ 关键词太严 ④ 发送者昵称一字不差 |
| 按发送者抓不到 | 跑 `python diag_sender.py "群名"` 看实际 sender 值，复制进 `rules.senders` |
| 汇总太长 | 自动分段（max_chars=1800）；或调小 `time_range_days` |
| 夜间不跑 | 正常，防封号机制（23:00-6:00 停轮），可改 `night_stop` |
| 微信自动升级了 | 降级回 4.1.7.30，关闭自动更新；升级会导致所有功能失效 |
| 数据库被锁 | 两个 pipeline 同时在跑；计划任务设"防重叠"（IgnoreNew） |

## 六、安全与合规（必读）

- ⚠️ **任何微信自动化都有封号风险**，本 Skill 已内置 7 道防封号机制，但无法保证 100% 安全
- 挂机号建议：已养号（非新号）、不要批量加群（一天 ≤5-10 个）、频率别激进
- 只存命中消息，数据不出本地
- 聊天原文只当数据处理，不进系统提示词（防 Prompt 注入）
- 抓取他人消息涉及隐私，请遵守法律法规与平台规则，风险自担

## 七、版本历史

| 版本 | 日期 | 变更 |
|---|---|---|
| v1.1 | 2026-08-23 | 新增**按发送者过滤**（rules.senders）；新增 diag_sender.py 诊断工具；文档体系完善 |
| v1.0 | 2026-08-22 | 由 wechat-message-capture v0.6.0 通用化沉淀：去业务化（关键词/汇总/分发全部用户自定义），全套防封号+心跳保留 |
