# wechat-message-capture · Skill 生命周期档案

> 本文档记录「微信消息抓取 Skill」从想法到成型的完整来龙去脉。
> 作用：以后看这份文档，即可了解本 Skill 的起源、演进、能力、坑与优化方向，无需翻历史会话。
> 最后更新：2026-08-23

---

## 一、起源（2026-08-10）

**前身项目**：微信群招聘信息聚合助手（代号 wechat-recruiter）

- **需求**：用户有 60+ 个建筑工地微信群，每天群里大量招聘信息（招铝模工/木工/焊工等），需要自动监测、抓取、汇总，每晚发回群或发给自己
- **原始决策**：专用小号挂机、主号永不接自动化；关键词粗筛（铝模/木工/日结/月结等）；每晚 22:00 汇总
- **用户原话**："不要着急开发，先把大纲和流程搞出来"

## 二、技术选型（2026-08-10 ~ 08-13）

| 方案 | 结论 | 原因 |
|---|---|---|
| **wxauto4 4.0（UI 自动化）** | ✅ 采用 | 封号风险最低，基于 Windows UIA 控件树，不碰协议 |
| wcferry（PC 微信 hook） | ❌ 放弃 | 仓库 2026-07-10 归档，只支持微信 3.9.x |
| wechaty/padlocal（协议库） | ❌ 放弃 | 封号风险高/付费 |

**核心架构**：wxauto 挂机轮询 → 关键词粗筛 → SQLite 落库（去重）→ 定时读库汇总 → 直发微信

## 三、技术攻坚（2026-08-13 ~ 08-17）

### 3.1 微信版本兼容（最大坑）
- 微信 4.1.12.55：自绘 UI，UIA 控件树不暴露内部控件 → **wxauto 完全不可用**
- **解决：降级微信到 4.1.7.30**（该版本消息列表暴露 UIA 控件）→ 从此锁定版本禁止升级

### 3.2 wxauto GetAllMessage 兼容 bug
- wxauto4 41.1.2 在 4.1.7.30 上 `GetAllMessage()` 返回 0 条（找不到 chat_message_list 引用）
- **解决**：双通道方案——优先 GetAllMessage（带消息对象，可读发送者/时间），失败回退自定义 UIA 枚举 `chat_message_list` 的 `ChatItemView.Name`
- 2026-08-14 实测确认 GetAllMessage 可用（前提：先 ChatWith 打开会话）

### 3.3 消息列表虚拟化
- RecyclerListView 只渲染可见消息 → 历史消息读不到
- **解决**：不滚动，靠高频轮询 + 增量去重兜底（遇已入库消息即停 = 增量边界）

### 3.4 大号切换（2026-08-17，重大变更）
- 原决策「主号永不接自动化」被用户推翻 → **换大号霸王别姬挂机**（接受封号风险）
- 大号 GetSession 返回 0（群都在"折叠的聊天"里）→ **改为 ChatWith 搜索式打开**，不依赖会话列表枚举
- 折叠聊天全量扫描 91 会话 → 筛选 → 用户拍板 **71 群**

## 四、业务规则迭代（2026-08-14 ~ 08-20）

过滤规则经历了多轮用户驱动的迭代（这些规则后来在通用化时被抽离）：

| 日期 | 迭代 | 内容 |
|---|---|---|
| 08-14 | 关键词扩充 | 木工/地下室/首拼/调平/散人/单兵/拉片/螺杆 |
| 08-14 | 材料厂家排除 | 直销/批发/现货/出售等强信号 + 厂家+卖货词组合信号 |
| 08-14 | 非招聘识别 | 转发说明排除（"以上信息均为其他群转发"）|
| 08-14 | 待确认分组 | 无电话/无强信号 → 汇总末尾"⚠️ 待确认"人工判断 |
| 08-17 | 广告 8 类目全剔 | 家政/服务/税务/工厂/海外/材料/团队强推销/找活接单；81→57 条 |
| 08-17 | 找活推销排除 | "我队现有/随时到位/老板联系我/闲N人"；57→49 条 |
| 08-17 | 只抓 3 天内 | max_days=3 时间窗口（防旧消息堆积）|
| 08-17 | 原文输出不简化 | 用户拍板：汇总按原文完整输出，不 LLM 简化 |
| 08-20 | 白名单思维重写 | is_building_related 建筑相关判定 80+ 词，非建筑全剔；36→31 条 |

## 五、防封号体系（持续演进）

| 机制 | 引入时间 | 说明 |
|---|---|---|
| 夜间停轮 23:00-6:00 | 08-17 | 深夜不轮询 |
| 随机采样 70% 群 | 08-17 | 每轮打乱规律 |
| 群间随机休息 3-8s | 08-17 | 模拟人类节奏 |
| 轮询随机抖动 ±10min | 08-17 | 固定间隔+随机偏移 |
| 关闭点资料卡 | 08-17 | 高频点头像弹窗是风控高危 |
| 锁微信版本 | 08-13 | 4.1.7.30 禁升级（wx_probe 监控） |
| 时间窗口 | 08-17 | 只读近 3 天 |
| 采样+休息节奏 | 08-17 | 折叠群扫描也用滚动+随机 |

## 六、通用化改造（2026-08-20，v0.6.0）

**用户拍板方向变更**：不打包招聘聚合，改打包**通用「微信聊天记录抓取」Skill**——其他人改 config 即可提取自己关心的信息。

- config 拆 **capture 层（通用）** + **rules 层（业务可配置）**
- filter.py 新增 `build_rules()` 从配置编译规则，未配置项回退默认
- 宠物行业模拟场景 4/4 通过 + 招聘回归 5/5 通过
- 观察期 8/21-8/22：检查点 cron 21:00 通过（稳定无错）

## 七、正式沉淀（2026-08-22 22:53）

- **skill_workshop 创建提案** `wechat-message-capture-20260822-f88c4b35b3` → 用户拍板沉淀
- 名字定为「**微信消息抓取 Skill**」（wechat-message-capture）
- **坑**：agent 工具 apply 报 "Plugin approval required (gateway unavailable)"（approvalPolicy=pending 弹窗未送达）→ **改用 CLI `openclaw skills workshop apply` 完成**（此后沿用此方法）

## 八、用户反馈 → 二次重构（2026-08-22 23:20）

**用户核心意见**：不要把现在的关键词、汇总和分发方式封装进 Skill！

→ 全面改成**安装时问答式配置**：
1. 抓哪些群/哪些人？→ `targets.groups/contacts`
2. 关键词/排除词？→ `rules.keywords/exclude_keywords`
3. 监测频率？→ `poll.interval_seconds`
4. 汇总发哪/频率/几点？→ `summary.*`
5. 抓几天内？→ `time_range_days`
+ 补充：是否带手机号/实时推送/数据保留/风控等级/告警对象

→ 新增《安装配置说明书.md》（问答清单）

## 九、真实场景测试 → 按发送者过滤（2026-08-22 23:46）

**用户真实需求**：抓「示例群」里「张三组长」（原：徒弟交流群/刘基森组长）的所有发言。

- **技术验证**：wxauto GetAllMessage 返回 sender 字段（群内昵称）✅ → 新增 `diag_sender.py` 诊断工具
- **新增功能**：`rules.senders` 按发送者过滤（is_sender_match；sender=None 保守拒绝）
- **实测**：读 9 条 → 目标 3 条全入库、他人 6 条全过滤 → 汇总直发文件传输助手成功
- 配置：10 分钟轮询 / 近 2 天 / 无排除词 → 写入 config 并验证

## 十、正式封装（2026-08-23 00:02）

- 清理全部测试残留（__pycache__/测试 db/测试日志）
- config.json 重置空白安装模板
- 新增完整《使用说明书.md》（功能/实现机制/用法/故障排查/安全合规/版本历史）
- skill_workshop 更新提案 apply（v1.1）

## 十一、开源发布（2026-08-23 00:18）

- 隐私清理：真实群名/人名 → 通用示例（张三组长/示例群）
- GitHub 公开仓库：**https://github.com/Youzi2010/wechat-message-capture**
- 上传 17 文件；沉淀《Skill上传GitHub标准流程.md》供以后复用
- 关键坑：git 直连被重置 → `-c http.version=HTTP/1.1` 解决

## 十二、当前能力（v1.1）

| 功能 | 说明 |
|---|---|
| 自动抓取 | wxauto4 轮询指定群/联系人最新消息（UIA 自动化，零协议风险） |
| 按发送者过滤 | rules.senders：只抓特定人发言 |
| 关键词/排除词 | rules.keywords / exclude_keywords |
| 增量落库 | SQLite + hash 去重 + 增量边界 |
| 时间窗口 | time_range_days（默认 3 天） |
| 定时汇总 | 原文输出、跨群归一化去重、自动分段直发 |
| 反封号 7 道 | 夜间停轮/随机采样/人类节奏/抖动/关资料卡/时间窗口/锁版本 |
| 心跳监控 | wx_probe 进程/版本/wxauto 三检查 |

## 十三、技术架构（当前）

```
微信 4.1.7.30 (UIA 控件树)
  └─ wechat_client.py: ChatWith 搜索打开 → GetAllMessage（含 sender）/ UIA 回退
      └─ pipeline.py: 随机采样 → 读消息 → 过滤（发送者→关键词→排除词）→ store.py SQLite 入库
          └─ gen_wechat_summary.py: 窗口查询 → 归一化去重 → 原文输出
              └─ send_wechat_summary.py: 分段(≤1800字) → send_via_wxauto.py 直发
```

## 十四、已知坑 & 注意事项（优化必读）

1. **微信版本是命门**：4.1.7.30 锁死；升级即全功能失效（wx_probe 盯防）
2. **GetAllMessage 依赖会话先打开**：read_messages 前必须 ChatWith + ChatInfo 验证当前会话
3. **消息列表虚拟化**：历史消息读不到，靠轮询频率兜底
4. **sender 依赖 GetAllMessage**：UIA 回退时无 sender，配置了 senders 会保守丢弃
5. **emoji/符号群名**：ChatWith 必须精确匹配（含 emoji）
6. **夜间停轮**：测试时要临时禁用（start=99），测完恢复
7. **PowerShell 写 JSON 带 BOM**：Python json.load 会失败，用 Python 写 config
8. **git 推 GitHub 被重置**：加 `-c http.version=HTTP/1.1`

## 十五、未来优化方向（待办）

- [ ] **实时推送模式**：抓到新消息即发（当前是定时汇总；实时模式风险更高需谨慎）
- [ ] **多接收人支持**：summary.send_to 支持列表（目前单个）
- [ ] **数据自动清理**：db 定期清理旧数据（目前手动）
- [ ] **消息滚动加载**：实现 RecyclerListView 滚动读历史（解决虚拟化限制）
- [ ] **发送者微信号**：enable_sender_wxid 打开时点资料卡（风控高，仅好友可见，需谨慎）
- [ ] **配置文件热更新**：改 config 自动 reload（目前每轮重新读，已基本满足）
- [ ] **失败重试与告警完善**：pipeline 异常自动重试 + 微信告警
- [ ] **安装向导脚本**：问答式交互生成 config.json（目前靠说明书手填）

## 十六、版本历史

| 版本 | 日期 | 里程碑 |
|---|---|---|
| wechat-recruiter v0.1-v0.5 | 08-10~08-19 | 建筑招聘专用：轮询/过滤/汇总/直发/防封号体系成型 |
| v0.6.0 | 08-20 | 通用化改造：config 拆 capture/rules 两层，build_rules 编译 |
| v1.0 | 08-22 22:53 | skill_workshop 沉淀，SKILL.md 大号版 |
| v1.0.1 | 08-22 23:20 | 用户反馈去业务化：问答式配置 + 安装说明书 |
| v1.1 | 08-23 00:02 | 按发送者过滤（senders）+ 完整使用说明书 + 正式封装 |
| v1.1 发布 | 08-23 00:18 | 开源到 GitHub（公开仓库） |

---

*档案结束。后续每次重大迭代，请在本文件追加记录（新章节或版本行）。*
